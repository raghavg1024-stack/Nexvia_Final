import type { Metadata } from "next";
export const metadata: Metadata = { title: "Explore Careers", description: "Explore career paths and connect them to a personalized Nexvia roadmap." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
